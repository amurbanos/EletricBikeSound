import React, {useState, useEffect} from 'react';
import {Text, View} from 'react-native';

const Splash = props => {
  const [vel, setVel] = useState(1);
  return (
    <>
      <Text>Oiii Genteeeem</Text>
    </>
  );
};

export default Splash;
